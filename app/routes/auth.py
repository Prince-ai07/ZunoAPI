# app/routes/auth.py
# Authentication routes for Zuno.
# Full code is written in Session 2.
# For now this placeholder allows the server to start.

from flask import Blueprint

auth_bp = Blueprint('auth', __name__)